# Host build-scripts (SPP `circuits` crate / circom parse) need a large stack on Windows MSVC.
# Also used when cargo-ndk cross-compiles: build.rs runs on host.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-arg=/STACK:0x20000000"]
