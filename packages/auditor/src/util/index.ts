/**
 * Shared utilities for the auditor pipeline (process spawning, evidence
 * capture, ISO timestamp helpers, atomic file writes).
 *
 * Implementations land alongside the passes that need them, beginning with
 * task 3.1 ("evidence-capture harness").
 */

export * from './errors';
